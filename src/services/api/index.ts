/**
 * Shared service layer.
 *
 * Place cross-feature API utilities here:
 * - Query key factories shared by multiple features
 * - Generic paginated fetch helpers
 * - Request/response transformers
 *
 * Endpoint-specific API functions belong inside each feature
 * (`features/<name>/api/`), not here.
 */
export {}
