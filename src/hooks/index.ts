/**
 * Shared application hooks.
 *
 * Generic, non-feature hooks live here (e.g. useDebounce, useMediaQuery,
 * useLocalStorage). Feature-specific hooks belong in
 * `features/<name>/hooks/`.
 */
export {}
