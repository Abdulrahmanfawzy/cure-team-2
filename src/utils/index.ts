/**
 * Pure utility helpers.
 *
 * Framework-agnostic functions only (formatters, mappers, guards).
 * UI hooks go in `hooks/`; shadcn `cn` stays in `lib/utils.ts`.
 *
 * Example:
 *   export function formatBytes(bytes: number) { ... }
 */
export {}
