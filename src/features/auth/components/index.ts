/**
 * Auth components.
 *
 * Feature-only UI lives here (forms, guards UI, profile menus, etc.).
 * Reusable pieces shared across features belong in `components/shared/`.
 * Primitives (Button, Input, Dialog, ...) come from `components/ui/`.
 *
 * Expected components when auth is implemented:
 * - login-form.tsx        — RHF + zodResolver login form
 * - register-form.tsx     — RHF + zodResolver registration form
 * - otp-verification.tsx  — Controller-wrapped InputOTP for codes
 * - auth-layout.tsx       — centered layout for auth pages
 */
export {}
