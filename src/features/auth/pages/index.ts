/**
 * Auth pages.
 *
 * Route-level components for the auth feature. The router currently uses
 * placeholders in `app/router/pages/`; swap them for these exports when
 * the feature is implemented.
 *
 * Expected pages:
 * - login-page.tsx
 * - register-page.tsx
 * - forgot-password-page.tsx
 */
export {}
