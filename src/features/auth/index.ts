/**
 * Auth feature — public API.
 *
 * Import from `@/features/auth` elsewhere in the app instead of reaching
 * into deep paths. Re-export only what other features/app code needs:
 *
 *   export { LoginPage, RegisterPage } from './pages'
 *   export { useLogin, useRegister } from './hooks'
 *   export type { User } from './types/auth-types'
 */
export {}
