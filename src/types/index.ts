/**
 * Global shared types.
 *
 * App-wide cross-cutting types live here (route params, global unions, etc.).
 * Feature-specific types belong in `features/<name>/types/`.
 * API envelope types live in `types/api.ts`.
 */
export {}
