/**
 * Application constants.
 *
 * Static values used across the app (roles, breakpoints, feature flags).
 * Secrets/env-driven values belong in `.env` (`import.meta.env.VITE_*`).
 *
 * Example:
 *   export const APP_NAME = 'Cure'
 *   export const DEFAULT_PAGE_SIZE = 20
 */
export const APP_NAME = 'Cure Starter'
